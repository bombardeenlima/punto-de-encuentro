# Example environment variables (copy to .env.local for local dev)

# Convex deployment used by `npx convex dev` (optional)
CONVEX_DEPLOYMENT=

# Public Convex URL used by the app (required for builds/deploys)
PUBLIC_CONVEX_URL=
